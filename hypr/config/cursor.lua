hl.env("XCURSOR_THEME", "Bibata-Modern-Ice")
hl.env("XCURSOR_SIZE", "24")

hl.config({
    cursor = {
        -- Set to false because Intel handles hardware cursors beautifully with zero lag
        no_hardware_cursors = false 
    }
})
